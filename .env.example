# Leave repo-relative paths empty unless you need to override the defaults.
PROJECT_ROOT=""
DIR_WORK=""
DATA_DIR=""
DOCS_DIR=""
PAPER_DIR=""
ARTIFACTS_DIR=""
DEFAULT_CONFIG_PATH=""
RAW_DATASET_PATH=""

# Keep the uv environment outside the repo so generated environments stay local.
UV_PROJECT_ENVIRONMENT="${HOME}/.venvs/reporting-risk-cascade"

# Local manuscript exports are intentionally outside the repo.
DIR_MANUSCRIPT="${HOME}/reporting-risk-cascade-manuscript"

SEED_DEFAULT="42"
